# 科研论文整理与总结网站 - 功能设计文档

## 核心功能模块

### 0. 用户系统

#### 注册功能
- **注册表单**
  - 用户名（唯一）
  - 邮箱
  - 密码（加密存储）
  - 确认密码
  - 所属部门/机构（可选）
- **邮箱验证**（可选）
  - 发送验证邮件
  - 激活账号
- **注册审核**（可选）
  - 管理员审核新用户
  - 审核通过后激活账号

#### 登录功能
- **登录方式**
  - 用户名/邮箱 + 密码登录
  - 记住登录状态
  - 忘记密码（邮件重置）
- **双重角色系统**
  - **管理员**：拥有系统管理权限
  - **普通员工**：拥有个人功能权限
- **会话管理**
  - Token/Session管理
  - 自动登录
  - 安全退出

#### 角色与权限

##### 管理员权限
- **用户管理**
  - 查看所有用户列表
  - 添加/编辑/删除用户
  - 重置用户密码
  - 启用/禁用用户账号
  - 审核新用户注册
- **系统管理**
  - 查看所有论文数据
  - 管理公共论文库
  - 查看系统统计数据（总用户数、论文数等）
  - 系统配置管理
  - 数据备份与恢复
- **权限管理**
  - 设置用户角色
  - 管理权限分配

##### 普通员工权限
- **个人功能**
  - 管理个人论文库（添加、编辑、删除自己的论文）
  - 创建和编辑个人笔记
  - 个人数据导出
  - 个人阅读统计
- **协作功能**（如果开启）
  - 查看共享论文库
  - 参与协作笔记
- **限制**
  - 无法访问其他用户的私有数据
  - 无法进行系统管理操作
  - 无法查看或管理其他用户

#### 个人中心
- **个人信息管理**
  - 修改个人资料
  - 修改密码
  - 头像上传
- **账号安全**
  - 登录日志查看
  - 活跃会话管理
  - 安全设置

#### 内容所有权与权限控制

##### 内容关联规则
- **所有内容与创建者关联**
  - 每篇论文记录添加者（创建者）信息
  - 每条笔记记录创建者信息
  - 每个标签记录创建者信息
  - 每个项目/主题记录创建者信息
  - 显示内容创建时间和最后修改时间
  - 显示内容创建者姓名/用户名

##### 内容操作权限
- **个人内容管理**
  - ✅ **可以修改**：用户自己创建的论文信息
  - ✅ **可以删除**：用户自己创建的论文记录
  - ✅ **可以修改**：用户自己创建的笔记
  - ✅ **可以删除**：用户自己创建的笔记
  - ✅ **可以修改**：用户自己创建的标签和分类
  - ✅ **可以删除**：用户自己创建的标签和分类
  - ❌ **无法修改**：其他用户创建的内容
  - ❌ **无法删除**：其他用户创建的内容

- **管理员特权**
  - ✅ 查看所有用户的内容
  - ✅ 修改任何用户的内容（需谨慎使用）
  - ✅ 删除任何用户的内容（需谨慎使用）
  - ✅ 查看内容的完整操作历史
  - ✅ 批量管理违规内容

##### 内容删除规则
- **软删除机制**（推荐）
  - 删除的内容不立即从数据库移除
  - 标记为"已删除"状态
  - 用户界面不再显示
  - 管理员可以恢复误删内容
  - 定期清理（如30天后永久删除）

- **级联删除规则**
  - 删除论文时，提示是否同时删除相关笔记
  - 删除项目时，提示是否同时删除项目内的论文关联
  - 删除用户账号时，选择保留或删除其创建的内容

---

### 1. 论文管理

#### 添加论文
- **手动输入论文信息**（标题、作者、期刊、年份等）
- **通过DOI/arXiv ID自动抓取元数据**
- **上传PDF文件并自动解析基本信息**
- **导入BibTeX/EndNote等格式**
- **自动记录添加者信息**（创建者ID、创建时间）

#### 论文库
- **列表/卡片视图**展示所有论文
  - 显示论文基本信息
  - **显示添加者信息**（添加者姓名、添加时间）
  - **显示操作按钮**（仅对自己添加的论文显示编辑/删除按钮）
- **多维度筛选**（按年份、期刊、作者、标签、添加者等）
- **搜索功能**（标题、关键词、笔记内容）
- **收藏/标星**重要论文
- **权限标识**
  - 自己添加的论文显示"可编辑"标识
  - 他人添加的论文显示"只读"或添加者名称

#### 论文编辑与删除
- **编辑功能**
  - 仅能编辑自己添加的论文
  - 修改论文元信息
  - 更新PDF文件
  - 记录修改历史（修改时间、修改内容）
- **删除功能**
  - 仅能删除自己添加的论文
  - 删除前二次确认
  - 提示是否同时删除相关笔记
  - 软删除机制（可恢复）

---

### 2. 阅读与笔记

#### 笔记系统
- **为每篇论文添加结构化笔记**：
  - 研究问题/动机
  - 核心方法
  - 主要结论
  - 创新点
  - 局限性
  - 个人思考
- **支持Markdown格式**
- **插入图片、公式、代码片段**
- **笔记所有权**
  - **自动记录笔记创建者**（创建者ID、创建时间）
  - **显示笔记作者信息**（作者姓名、创建/修改时间）
  - **仅创建者可编辑/删除**自己的笔记
  - **其他用户只读**他人的笔记（如果共享）

#### 笔记管理
- **编辑笔记**
  - 仅能编辑自己创建的笔记
  - 实时保存或手动保存
  - 保存修改历史记录
  - 显示最后修改时间
- **删除笔记**
  - 仅能删除自己创建的笔记
  - 删除前确认
  - 软删除机制
- **笔记可见性**（可选功能）
  - 私有笔记：仅自己可见
  - 团队共享：团队成员可查看（只读）
  - 协作笔记：允许他人编辑（需特殊权限设置）

#### 高亮与标注
- **PDF在线阅读器**（如果存储了PDF）
- **重要段落高亮**
  - 高亮与用户账号关联
  - 仅显示自己的高亮标记
  - 可修改/删除自己的高亮
- **添加批注**
  - 批注与用户账号关联
  - 显示批注作者信息
  - 仅能编辑/删除自己的批注

---

### 3. 组织与分类

#### 分类体系
- **自定义标签系统**
  - 创建个人标签
  - 标签与创建者关联
  - 仅能修改/删除自己创建的标签
  - 可查看系统公共标签（管理员创建）
- **研究主题分类**
  - 创建个人研究主题
  - 主题与创建者关联
  - 仅能管理自己的主题分类
- **项目/课题分组**
  - 创建个人项目分组
  - 项目与创建者关联
  - 仅能编辑/删除自己的项目
  - 可选：项目成员协作（多人共享项目）
- **阅读状态标记**（待读/在读/已读）
  - 状态标记与用户关联
  - 每个用户独立管理自己的阅读状态

#### 关联关系
- **论文之间的引用关系**
- **相关论文推荐**
- **论文关系图谱可视化**

---

### 4. 总结与分析

#### 个人总结
- **按主题生成论文综述**
- **研究脉络梳理**
- **方法对比表格**

#### 统计分析
- **阅读量统计**（按时间、主题）
- **研究热点词云**
- **作者/期刊分布**

---

### 5. 导出与分享

#### 导出功能
- **导出笔记为PDF/Word**
- **生成参考文献列表**（多种格式）
- **导出某个主题的所有论文及笔记**

#### 分享协作（可选）
- **与团队成员共享论文库**
- **协作笔记**
- **评论与讨论**

---

### 6. AI助手（LLM集成）

#### AI助手界面窗口
- **固定浮动窗口**
  - 页面右下角/侧边栏固定位置
  - 可展开/收起的对话窗口
  - 支持拖拽调整位置和大小
  - 最小化后显示图标气泡提示

- **对话界面设计**
  - **输入区域**
    - 多行文本输入框
    - 支持Markdown格式
    - 文件/图片上传（上传论文片段）
    - 快捷操作按钮
  - **对话区域**
    - 聊天式对话界面
    - 显示用户问题和AI回答
    - 支持代码块、公式渲染
    - 回答可复制、引用、保存
  - **上下文选择**
    - 选择当前论文作为上下文
    - 选择某个项目/主题的所有论文
    - 选择特定笔记作为参考

#### AI助手功能

##### 1. 论文理解与问答
- **论文内容问答**
  - 对上传的PDF或已添加的论文进行提问
  - 提取论文关键信息
  - 解释专业术语和概念

- **快捷提问模板**
  - "总结这篇论文的核心内容"
  - "这篇论文的创新点是什么？"
  - "论文使用了什么研究方法？"
  - "解释论文中的XX概念"

##### 2. 笔记辅助撰写
- **智能笔记生成**
  - 基于论文内容生成结构化笔记框架
  - 提取研究问题、方法、结论
  - 生成思维导图大纲

- **笔记优化**
  - 润色和改进用户的笔记
  - 补充遗漏的要点
  - 格式化笔记内容

##### 3. 文献综述与总结
- **多篇论文对比分析**
  - 对比多篇论文的方法和结论
  - 生成文献综述
  - 梳理研究脉络和发展趋势

- **主题总结**
  - 基于某个项目/主题的所有论文生成总结
  - 生成研究现状报告
  - 识别研究空白

##### 4. 写作辅助
- **参考文献格式化**
  - 生成各种格式的引用（APA、MLA、Chicago等）
  - 自动整理参考文献列表

- **学术写作建议**
  - 论文写作指导
  - 研究思路建议
  - 实验设计建议

##### 5. 翻译与解释
- **论文翻译**
  - 中英文互译
  - 专业术语翻译

- **概念解释**
  - 解释复杂的学术概念
  - 提供相关背景知识

#### AI助手权限与配置

- **用户级别权限**
  - **普通员工**：有使用次数限制（如每日50次调用）
  - **管理员**：无限制或更高额度

- **数据隐私**
  - 用户可选择是否将论文内容发送给LLM
  - 敏感内容警告提示
  - 对话历史仅用户本人可见

- **模型选择**（管理员配置）
  - 选择使用的LLM模型（GPT-4、Claude、本地模型等）
  - 配置API密钥
  - 调整模型参数（温度、最大长度等）

#### 技术实现

- **LLM集成方式**
  - 调用OpenAI API / Anthropic API
  - 集成开源模型（Llama、ChatGLM等）
  - 支持本地部署模型

- **上下文管理**
  - RAG（检索增强生成）技术
  - 向量数据库存储论文内容
  - 智能检索相关内容作为上下文

- **会话管理**
  - 保存对话历史
  - 会话上下文延续
  - 多轮对话支持

---

## 用户工作流程设计

### 新用户流程
1. **注册账号**
2. **等待审核**（如果开启审核功能）
3. **登录系统**
4. **完善个人信息**

### 日常使用流程
1. **登录系统**
2. **添加论文**
3. **标记分类**
4. **阅读并记笔记**
   - 使用AI助手理解论文内容
   - 让AI助手生成笔记框架
   - AI辅助翻译和概念解释
5. **建立关联**
6. **定期总结**
   - 使用AI助手生成文献综述
   - AI辅助对比分析多篇论文
7. **导出使用**

### 管理员流程
1. **登录系统**
2. **审核新用户**（如需要）
3. **管理用户账号**
4. **查看系统统计**
5. **管理公共论文库**
6. **系统配置与维护**
7. **配置AI功能**（模型选择、API密钥、用户额度设置）

---

## 辅助功能

### 提醒系统
- 阅读计划提醒
- 定期回顾提醒

### 快速操作
- 快捷键支持
- 批量操作（标签、移动、删除）

### 数据管理
- 云端同步
- 数据备份导出
- 隐私设置

---

## 界面布局建议

### 公共页面（未登录）

1. **登录页** - 用户名/密码登录、记住我、忘记密码
2. **注册页** - 注册表单、用户协议
3. **密码重置页** - 邮箱验证、设置新密码

### 普通员工页面

1. **仪表盘** - 最近阅读、阅读统计、待办事项
2. **论文库** - 所有论文的管理界面
3. **论文详情页** - 元信息 + 笔记 + PDF查看
4. **主题/项目页** - 按研究主题组织的视图
5. **总结页** - 生成和管理综述性内容
6. **个人中心** - 个人资料、密码修改、安全设置
7. **AI助手窗口** - 固定浮动窗口（所有页面可用）

### 管理员专属页面

1. **用户管理页** - 用户列表、添加/编辑/删除用户、权限设置
2. **用户审核页** - 待审核用户列表、审核操作
3. **系统统计页** - 全局数据统计、用户活跃度、系统使用情况
4. **系统设置页** - 全局配置、功能开关、系统维护
5. **AI配置页** - LLM模型选择、API配置、使用额度管理

### 全局组件

1. **AI助手浮动窗口**
   - 位置：页面右下角固定位置
   - 状态：最小化图标 / 展开对话窗口
   - 功能：
     - 展开后显示完整聊天界面
     - 支持拖拽移动位置
     - 支持调整窗口大小
     - 显示未读消息提示
   - 可用范围：登录后所有页面

---

## 界面视觉设计要求

### 图片与视觉资源

#### 资源来源
- **公开免费图片资源平台**
  - Unsplash（高质量免费图片）
  - Pexels（免费商用图片库）
  - Pixabay（免费图片和插画）
  - Freepik（图标、插画、背景）
  - unDraw（开源插画库）

- **AI生成图片资源**
  - 使用 Midjourney / DALL-E / Stable Diffusion 等AI工具生成
  - 生成主题相关的学术、科研、阅读场景图片
  - 生成抽象背景、装饰元素
  - 生成用户默认头像（AI生成的抽象头像）

#### 应用场景

##### 1. 页面装饰性图片
- **登录/注册页背景**
  - 科研实验室、图书馆、书籍场景图片
  - AI生成的学术氛围背景图

- **空状态插画**
  - 论文库为空时的提示插画
  - 无搜索结果时的友好提示图
  - 无笔记时的引导插画
  - 使用 unDraw 或 AI生成的插画

- **引导页/欢迎页**
  - 新用户引导的配图
  - 功能介绍的插画

##### 2. 占位图与缺省图
- **论文封面占位图**
  - 未上传PDF时使用默认学术主题图片
  - 可根据论文类别使用不同占位图

- **用户头像**
  - 默认头像使用AI生成的几何图案或抽象头像
  - 或使用 DiceBear Avatars 等头像生成服务

##### 3. 图标与UI元素
- **图标库**
  - Font Awesome（免费图标库）
  - Material Icons（Google Material Design图标）
  - Heroicons（Tailwind CSS图标）
  - Feather Icons（极简图标）

- **插画与装饰**
  - 仪表盘统计图表的装饰性元素
  - 功能模块的配图

##### 4. 背景与纹理
- **页面背景**
  - 使用 SVG 几何背景生成器
  - AI生成的抽象纹理背景
  - 渐变色背景

### 视觉风格建议

#### 色彩方案
- **学术专业风格**
  - 主色：深蓝色/墨绿色（专业、学术）
  - 辅色：浅灰色/米白色（简洁、清爽）
  - 强调色：橙色/金色（重点突出）

#### 设计原则
- **简洁专业**：避免过度装饰，保持学术氛围
- **一致性**：所有图片和插画风格保持统一
- **适度使用**：图片作为辅助，不影响内容阅读
- **响应式**：图片适配不同屏幕尺寸

### 资源管理

#### 版权与授权
- **确保所有资源符合商用授权**
  - 优先使用 CC0（公共领域）资源
  - 使用 Creative Commons 许可的资源
  - AI生成的资源需确认使用权限

- **资源归属标注**（可选）
  - 在页面底部标注图片来源
  - 遵守相关平台的使用条款

#### 本地化存储
- **CDN加速**
  - 图片资源托管到CDN
  - 优化加载速度

- **图片优化**
  - 压缩图片文件大小
  - 使用 WebP 等现代格式
  - 实现懒加载

---

## 技术考虑点

- **用户认证与授权**：
  - 密码加密（bcrypt/argon2）
  - JWT Token 或 Session 管理
  - 基于角色的访问控制（RBAC）
  - 安全的密码重置机制

- **数据关联与权限控制**：
  - **外键关联设计**
    - 论文表关联创建者ID（user_id）
    - 笔记表关联创建者ID
    - 标签/项目表关联创建者ID
    - 所有内容表包含 created_by、created_at、updated_at 字段
  - **权限验证机制**
    - 后端API层面验证用户权限
    - 前端UI层面显示/隐藏操作按钮
    - 中间件拦截未授权操作
  - **数据查询过滤**
    - 查询时自动过滤创建者条件（WHERE created_by = current_user_id）
    - 管理员可查询所有数据（跳过过滤）

- **数据存储**：
  - **数据库选型（推荐使用嵌入式数据库）**
    - **SQLite**（强烈推荐）
      - 无需额外安装，零配置
      - 基于文件的数据库，数据存储在单个.db文件中
      - 支持完整的SQL功能和事务
      - 适合中小型应用（支持TB级数据）
      - 跨平台，性能优秀
      - 支持外键、索引、触发器等高级功能
      - 内置全文搜索（FTS5扩展）
      - 数据库文件可直接备份和迁移
    - 数据库文件位置：`/data/database.db`

  - **存储内容**
    - 用户信息、论文元数据、笔记内容
    - PDF文件路径（PDF文件本身存储在文件系统）
    - 数据隔离（用户数据相互隔离）
    - 软删除字段（deleted_at, is_deleted）
    - 操作历史记录表（审计日志）

  - **文件存储结构**
    ```
    项目根目录/
    ├── data/
    │   ├── database.db          # SQLite数据库文件
    │   ├── uploads/             # 用户上传文件
    │   │   ├── pdfs/            # PDF文件
    │   │   │   └── user_123/    # 按用户ID组织
    │   │   └── avatars/         # 用户头像
    │   └── backups/             # 数据库备份
    ```

  - **未来扩展性**
    - 初期使用SQLite快速开发
    - 数据量大时可迁移到PostgreSQL/MySQL
    - 使用ORM（如SQLAlchemy/Prisma）便于数据库切换

- **搜索引擎**：
  - **SQLite全文搜索（FTS5）**
    - 使用SQLite内置的FTS5扩展
    - 支持中文分词（可集成jieba等分词库）
    - 支持多字段搜索（标题、作者、关键词、笔记内容）
    - 权限过滤（仅搜索用户有权访问的内容）
  - **搜索优化**
    - 为常用字段创建索引
    - 搜索结果高亮显示
    - 搜索历史记录

- **文件处理**：PDF解析、元数据提取

- **API集成**：与学术数据库对接（CrossRef、arXiv等）

- **邮件服务**：账号验证、密码重置邮件发送

- **安全性**：
  - SQL注入防护
  - XSS防护
  - CSRF防护
  - 请求频率限制
  - 敏感操作二次验证

- **LLM集成与AI功能**：
  - **API集成**
    - OpenAI API / Anthropic API 集成
    - API密钥安全存储（环境变量/加密存储）
    - API调用错误处理和重试机制
  - **向量数据库**
    - 使用 Pinecone / Weaviate / Milvus 等向量数据库
    - 论文内容向量化存储
    - 语义搜索和相似度匹配
  - **RAG（检索增强生成）**
    - 文档分块策略
    - 嵌入模型选择（OpenAI Embeddings / 开源模型）
    - 上下文检索优化
  - **使用额度管理**
    - 用户调用次数统计
    - 每日/每月额度限制
    - Token消耗追踪
    - 费用统计
  - **会话管理**
    - 对话历史存储
    - 会话上下文维护
    - 会话超时处理
  - **隐私保护**
    - 用户数据发送前的脱敏处理
    - LLM调用日志记录
    - 数据传输加密

---

## 后续规划

基于以上设计，可以进一步细化：

### 数据库表结构设计

#### 数据库说明
- **使用SQLite数据库**，文件位置：`/data/database.db`
- **数据类型映射**：
  - id: INTEGER PRIMARY KEY AUTOINCREMENT
  - 字符串: TEXT
  - 整数: INTEGER
  - 浮点数: REAL
  - 日期时间: TEXT (ISO 8601格式) 或 INTEGER (Unix时间戳)
  - 布尔值: INTEGER (0/1)
- **外键约束**：需要启用 `PRAGMA foreign_keys = ON;`
- **索引优化**：为常用查询字段创建索引

#### 核心表结构示例

1. **用户表（users）**
   - id (INTEGER PRIMARY KEY), username (TEXT UNIQUE), email (TEXT UNIQUE)
   - password (TEXT), role (TEXT), status (TEXT)
   - created_at (TEXT), updated_at (TEXT)

2. **论文表（papers）**
   - id (INTEGER PRIMARY KEY), title (TEXT), authors (TEXT)
   - journal (TEXT), year (INTEGER), doi (TEXT), pdf_path (TEXT)
   - **created_by** (INTEGER, 外键关联 users.id)
   - created_at (TEXT), updated_at (TEXT), deleted_at (TEXT)

3. **笔记表（notes）**
   - id, paper_id, content, note_type
   - **created_by** (外键关联 users.id)
   - created_at, updated_at, deleted_at

4. **标签表（tags）**
   - id, name, color
   - **created_by** (外键关联 users.id)
   - created_at, updated_at

5. **论文-标签关联表（paper_tags）**
   - id, paper_id, tag_id
   - **created_by** (外键关联 users.id)

6. **操作日志表（audit_logs）**（可选）
   - id, user_id, action, table_name, record_id, old_value, new_value, created_at

7. **AI对话会话表（ai_conversations）**
   - id, user_id, title, context_type, context_id
   - **created_by** (外键关联 users.id)
   - created_at, updated_at

8. **AI对话消息表（ai_messages）**
   - id, conversation_id, role (user/assistant), content, tokens_used
   - created_at

9. **用户AI额度表（user_ai_quotas）**
   - id, user_id, daily_limit, monthly_limit, daily_used, monthly_used
   - reset_date, created_at, updated_at

10. **论文向量表（paper_vectors）**（如果使用本地向量存储）
    - id, paper_id, chunk_index, content_chunk, embedding_vector
    - created_at

### API接口设计

**基本API规范**：
- RESTful API 规范
- 统一的权限验证中间件
- 统一的响应格式

**AI助手相关API**：
- `POST /api/ai/chat` - 发送消息给AI助手
- `GET /api/ai/conversations` - 获取用户的对话历史
- `GET /api/ai/quota` - 查询用户AI使用额度
- `POST /api/ai/conversations/:id/context` - 设置对话上下文
- `DELETE /api/ai/conversations/:id` - 删除对话记录

### 前端页面原型
- 用户界面设计
- 权限相关的UI交互设计

### 技术栈选型

#### 推荐技术栈（零依赖部署方案）

**后端框架**：
- **Python + Flask/FastAPI**
  - Flask：轻量级，易上手，插件丰富
  - FastAPI：现代化，支持异步，自动生成API文档
  - 优势：Python生态丰富，便于集成AI/ML库

- **Node.js + Express/NestJS**
  - Express：简洁灵活
  - NestJS：TypeScript支持，结构化
  - 优势：前后端统一语言，npm生态强大

**前端框架**：
- **React + Vite**：现代化开发体验，组件生态丰富
- **Vue 3 + Vite**：渐进式，上手容易，中文文档完善
- **UI组件库**：
  - Ant Design / Material-UI / shadcn/ui
  - Tailwind CSS（原子化CSS）

**数据库**：
- **SQLite**（强烈推荐）
  - 零配置，无需额外安装
  - 数据存储在项目的 `/data/database.db` 文件中
  - ORM推荐：
    - Python: SQLAlchemy / Peewee
    - Node.js: Prisma / Sequelize / TypeORM

**AI/LLM集成**：
- **API调用**：OpenAI SDK / Anthropic SDK
- **向量数据库**：
  - ChromaDB（可嵌入式部署，无需外部服务）
  - 或使用SQLite的向量扩展（sqlite-vss）
- **PDF处理**：PyPDF2 / pdf.js

**其他依赖**：
- **文件上传**：multer (Node.js) / Flask-Upload
- **身份认证**：JWT (jsonwebtoken)
- **邮件发送**：nodemailer / Flask-Mail

#### 零依赖部署优势

**所有服务都在项目内运行，无需外部依赖**：
1. ✅ 数据库：SQLite（文件数据库）
2. ✅ 文件存储：本地文件系统
3. ✅ 会话管理：文件/SQLite存储
4. ✅ 向量数据库：ChromaDB嵌入式模式

**部署方式**：
- 单一可执行文件或Docker容器
- 所有数据存储在 `/data` 目录
- 方便迁移和备份（只需复制 `/data` 目录）

**项目启动流程**：
```bash
# 1. 安装依赖
npm install  # 或 pip install -r requirements.txt

# 2. 初始化数据库（自动创建SQLite文件）
npm run db:init

# 3. 启动服务
npm run dev

# 无需安装MySQL、PostgreSQL、Redis等外部服务！
```


